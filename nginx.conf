server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Serve all static frontend files
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Block direct access to Python backend source
    location ~ ^/sim/ {
        return 404;
    }
}
